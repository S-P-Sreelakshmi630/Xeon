const RightSidebar = ({ user, transactions, banks }) => {
  return (
    <aside className="right-sidebar fixed right-0">
      <section className="flex flex-col pb-8">
        <div className="profile-banner">
          
        </div>
      </section>
      <section>
        
      </section>
    </aside>
  );
};

export default RightSidebar;
